/**
 * 状态枚举
 */
export const enum StatusEnum {
  FAIL = "failed",
  CANCEL = "canceled",
  ANALYSIS_ING = "pending",
  SUCCESS = "success",
  RUNNING = "running",
}

export const enum MenuType {
  KL_FILE = "klFile",
  KL_CONFIG = "klConfig",
}
